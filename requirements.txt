torch
torchvision
transformers
segment-anything
ultralytics>=8.4.115  # SAM3SemanticModel.mask_threshold 는 8.4.115부터
# SAM 3 (models/sam3.pt 존재 시)에만 필요한 두 줄 — 없으면 GDINO+SAM 경로만 사용
timm
clip @ git+https://github.com/ultralytics/CLIP.git
fastapi
uvicorn[standard]
python-multipart
pillow
opencv-python
numpy
pycocotools
supervision
requests
onnx
onnxscript
onnxruntime
pytest
httpx
# 선택: VLM 문맥 심판의 Claude API 제공자 (ANTHROPIC_API_KEY 필요).
# 미설치여도 도구는 정상 동작 — Ollama 폴백 또는 기능 비활성.
# anthropic
